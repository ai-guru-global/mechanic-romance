# GTM 页面实施计划

> **给执行代理：** 必需子技能：使用 superpowers:subagent-driven-development（推荐）或 superpowers:executing-plans 按任务逐个执行本计划。步骤使用复选框（`- [ ]`）语法跟踪。

**Goal:** 新建 `gtm/README.md`（六节单页：双线发布策略与可信度门禁）+ 根 README 知识地图插入一行 `gtm/` 导航。

**Architecture:** 纯 Markdown 文档任务，无代码。`gtm/README.md` 为新文件，遵循 spec 六节结构与「引用不复制」原则（进度数字一律链接 `docs/roadmap.md`，不出现第三处状态事实源）；根 README 仅插入两行（分隔行 + gtm/ 行），不动其他内容。

**Tech Stack:** GFM（GitHub Flavored Markdown），遵循 `docs/conventions.md`。

## Global Constraints

- 全文中文；标题不跳级（`#` 文件标题、`##` 六节、`###` 子节）。
- 目录与文件名英文连字符：新增目录为 `gtm/`。
- 引用不复制：页面不出现篇数 / 进度等动态数字；进度只链接 `docs/roadmap.md`。
- 无来源断言标注「（经验推测，待核实）」。
- 引用网页标 URL + 访问日期（本页预期不引外链 URL，渠道只写名称不编造链接）。
- 每个任务独立提交，提交信息为中文一句话，风格对齐仓库近期提交（如「GTM 页新增：…」）。
- spec：`docs/superpowers/specs/2026-09-03-gtm-page-design.md`（已批准并提交）。

---

### Task 1: 创建 gtm/README.md（六节单页）

**Files:**
- Create: `gtm/README.md`

**Interfaces:**
- Consumes: spec 六节结构与内容原则；仓库现状核实结论（决定「求职线现在可用」依据表述与门禁 checkbox 初始勾选状态）。
- Produces: `gtm/README.md`（Task 2 的导航行指向它；后续根 README 读者经此页理解双线策略）。

- [ ] **Step 1: 落笔前核实（spec 内容原则 3）**

依次运行以下命令，记录结论。核实结果决定 Step 2 中两处内容：①「时机判断」里「P0 可信度问题已修复」的表述是否属实；②门禁 6 项 checkbox 的初始状态（已达成的写 `[x]`）。

```bash
# 1. 参考节覆盖：总数 vs 有参考节的笔记数（「P0 已修复」依据之一）
find corpus -name "*.md" | wc -l
grep -rlE '^#{2,3} *(参考|引用|来源)' corpus --include='*.md' | wc -l

# 2. 状态一致性：README 与 roadmap 的里程碑状态是否一致（「P0 已修复」依据之二）
grep -nE 'M[0-4]' docs/roadmap.md | head -20
grep -nE 'M[0-4] ·' README.md | head -10

# 3. 死链遗留抽查：正文相对链接是否有明显断链模式（49c4acc 已清理过一轮）
grep -rnE '\]\((\.\./)*(corpus|demo|docs|research)/[^)]*\)' README.md | grep -vE 'http' | head -20

# 4. 门禁项现状盘点
grep -n "待核实" README.md | head          # 门禁第 4 项：时间线待核实条目
find demo -type f | head -20               # 门禁第 5 项：demo/assets 是否有真实素材
ls LICENSE* 2>/dev/null; grep -n "license" README.md | head -5   # 门禁第 6 项
grep -n "状态" "demo/scenarios/01-扩散策略-桌面堆叠/README.md" | head -3   # 门禁第 1 项：场景 01 状态
```

判定规则：
- 若参考节覆盖接近全量（≥ 60/67）且 README/roadmap 状态一致 → Step 2 中「P0 可信度问题已修复」表述保留，并如实列举。
- 若有明显缺口 → 把该表述改为按现状描述（如「主要笔记参考节已补齐，剩余 X 类待补」），不写未验证声明；对应门禁项保持 `[ ]`。
- 核实中若发现某门禁项当前已达成 → 在 Step 2 中将其标 `[x]` 并在该行尾注「（已于 2026-09 核实达成）」。

- [ ] **Step 2: 写入 gtm/README.md 全文**

创建 `gtm/README.md`，内容如下（门禁 checkbox 默认全部 `[ ]`，按 Step 1 核实结论调整勾选状态；正文其他文字按 Step 1 判定规则微调）：

````markdown
# 发布与推广 · Go-to-Market

> 一句话：求职线现在就能用，社区线等可信度门禁全绿再启动。

---

## 定位

本项目对外传播分两条线，各有一句话定位：

**求职线（招聘方 / 面试官 / 猎头）**

> 把具身智能研究组织成可复现、可讲解语料库的独立研究项目。卖点是研究组织力与诚实标注——「待核实」就标待核实，设计文档就明说是设计文档。

**社区线（开源社区）**

> 中文最系统的具身智能研究语料库 + Demo 场景工作台。卖点是可检索、互链、统一模板——看完即懂，讲完即用。

两条线共用同一个仓库，但启动时机不同：求职线现在可用，社区线等 [可信度门禁](#可信度门禁) 全部达成。

---

## 时机判断

### 求职线为什么现在可用

- P0 可信度问题已修复：硬件 / 行业 / 概念 / 方法笔记参考节补齐、死链清理、状态矛盾消除（2026-09 上旬的系列提交，见 git log）。
- 语料 + 场景定义足以展示研究组织能力——求职评估看的是「怎么组织知识」，不是「代码能不能跑」。
- 定位诚实：仓库当前就是设计文档 + 语料库，简历与面试如实说明，经得起追问。

### 社区线为什么等 M3

- 开源社区的第一印象只有一次：「0 行代码」在大流量渠道容易被定性为 PPT 项目。
- 首个可跑复现（[docs/roadmap.md](../docs/roadmap.md) M3）是最小可信度证明——「说到的能跑」比「写得多」值钱。
- 门禁未过就投大流量渠道，印象成本不可逆。

### 时机表

| 阶段 | 该做什么 | 面向谁 |
| --- | --- | --- |
| 现在（2026-09，M2 进行中） | 求职线全开：简历项目条目、面试讲解、猎头一页纸 | 招聘方 / 面试官 / 猎头 |
| M2 收尾 | 求职线继续；私下半公开——把仓库链接直接发给感兴趣的同行，不发公开帖 | 一对一接触的同行 |
| M3 落地 | 可信度门禁清零后，社区首发帖 | 开源社区 |
| M4 完成 | Demo 视频 / 阶段报告产出后二次传播 | 全受众 |

### 反模式

- ❌ 门禁未过就投掘金 / V2EX 等大流量渠道——开源社区的一次性印象成本不可逆。
- ❌ 简历 / 面试吹「场景都能跑」——一问代码就露馅；诚实定位反而加分。
- ❌ 等「全部做完」才求职——求职线不依赖 M3，语料库本身就是交付物。

---

## 可信度门禁

社区公开推广启动前，以下 6 项必须全部达成：

- [ ] 场景 01 达成最小可跑复现（以 [docs/roadmap.md](../docs/roadmap.md) M3 定义为准；场景定义见 [demo 01](../demo/scenarios/01-扩散策略-桌面堆叠/README.md)）
- [ ] README 数字与仓库实际一致
- [ ] 全部笔记有参考节且无死链
- [ ] 时间线「待核实」条目已核实或移除
- [ ] `demo/assets` 至少 1 个真实素材（图或视频）
- [ ] LICENSE 与引用方式明确

---

## 渠道与动作

### 求职线（现在激活）

**简历项目栏话术**（一句话 + 仓库链接 + 状态标签，篇数不写死、以仓库 README「30 秒读懂」为准）：

> 具身智能研究语料库「机械浪漫」：方法 / 硬件 / 仿真 / 概念多主题研究笔记 + 8 大类 Demo 场景设计文档，统一写作与引用规范（github.com/allengaller/mechanic-romance）｜状态：语料库完成，场景复现进行中

**面试 STAR 要点**：

- **S / T（为什么做）**：论文在 arXiv、代码在 GitHub、笔记在 Notion，三岛割裂，研究知识无法检索与复用。
- **A（怎么组织）**：统一场景模板（任务 / 观测 / 评估 / 硬件齐备）、统一引用规范（BibTeX + 来源 URL + 访问日期，见 [docs/conventions.md](../docs/conventions.md)）、诚实标注（待核实条目明示、场景状态透明）。
- **R（结果）**：规模与进度以根 README「[30 秒读懂](../README.md#30-秒读懂)」与 [docs/roadmap.md](../docs/roadmap.md) 为准——面试时打开仓库现场讲，不背数字。

**猎头 / 内部转岗一页纸**：直接引用本页「定位」一节的两句话 + 仓库链接。

### 社区线（门禁通过后激活）

**渠道清单**（附各自适配的内容形态）：

| 渠道 | 适配内容形态 |
| --- | --- |
| 掘金 | 首发长文：完整讲清痛点 + 结构 + 场景表 |
| 知乎 | 长文 + 「如何评价具身智能语料库」类回答引流 |
| V2EX | 分享创造节点短帖：链接 + 三句话价值主张 |
| LeRobot Discord | 英文短介绍 + 具体场景链接，垂直受众 |

**首发帖结构**：

1. 痛点：论文、代码、笔记三岛割裂；
2. 是什么：可检索语料库 + 场景工作台，配知识地图截图；
3. 场景全景：引用根 README 的 8 场景表，不复制；
4. 诚实说明：当前状态、roadmap、门禁剩余项；
5. 求反馈：明确问「哪里最值得先做」。

> 凡涉及「几点发帖曝光最好」之类的发布时间窗口经验断言，一律标注「（经验推测，待核实）」后再写。

---

## 里程碑触发器

| 里程碑 | 触发的 GTM 动作 |
| --- | --- |
| M2 收尾 | 求职线进入一对一私享：把仓库链接发给感兴趣的同行 / 面试官 |
| M3 落地 | 核对可信度门禁 6 项，全绿则社区首发帖（掘金 / 知乎 / V2EX 同步，LeRobot Discord 补英文帖） |
| M4 完成 | Demo 视频 / 阶段报告产出后二次传播，社区帖更新进展 |

---

## 元信息

- 状态：求职线已激活；社区线待可信度门禁
- 最后更新：2026-09-03
- 本页不复制进度数字：项目进度唯一事实源为 [docs/roadmap.md](../docs/roadmap.md)，仓库规模见根 README「[30 秒读懂](../README.md#30-秒读懂)」表
````

- [ ] **Step 3: 验证**

```bash
# 1. 六节齐全（应输出 6）
grep -c '^## ' gtm/README.md

# 2. 无占位符（应无输出；「待核实」是允许的标注词，不在模式内）
grep -nE 'TBD|TODO|占位|待补|XXX|待填' gtm/README.md

# 3. 仓库内相对链接全部存在（每行应输出 OK）
grep -oE '\]\((\.\./[^)#]+)' gtm/README.md | sed 's/](//' | sort -u | while read -r p; do test -f "$p" && echo "OK $p" || echo "MISSING $p"; done

# 4. 不出现独立状态数字（篇数/家数类，应无输出）
grep -nE '[0-9]+ ?(篇|家公司|个数据集|个基准)' gtm/README.md

# 5. 目录名合规（gtm 为英文连字符，文件唯一）
ls gtm/
```

预期：1 → `6`；2 → 无输出；3 → 全部 `OK`；4 → 无输出；5 → `README.md`。

- [ ] **Step 4: 提交**

```bash
git add gtm/README.md
git commit -m "GTM 页新增：双线发布时机与可信度门禁单页（gtm/README.md）"
```

---

### Task 2: 根 README 知识地图插入 gtm/ 导航行

**Files:**
- Modify: `README.md`（仅「🗺 知识地图」目录树，research/ 块与 docs/ 块之间）

**Interfaces:**
- Consumes: Task 1 产出的 `gtm/README.md`（导航行的指向目标）。
- Produces: 根 README 树含 `gtm/` 行，读者可从知识地图进入 GTM 页。

- [ ] **Step 1: 编辑插入**

使用 Edit 工具，`old_string` → `new_string`：

old_string：
```
│   └── experiments/               ← 实验记录
│
├── 📖 docs/                ← 项目级文档
```

new_string：
```
│   └── experiments/               ← 实验记录
│
├── gtm/                    ← 发布与推广策略（GTM）
│
├── 📖 docs/                ← 项目级文档
```

（`gtm/` 后恰为 20 个空格，使 `←` 与其他行注释列对齐——树中注释起始显示列统一为第 28 列。）

- [ ] **Step 2: 验证树形连贯与对齐**

```bash
# 显示树段落人工核对：gtm 行夹在 research 与 docs 之间，│ 分隔完整，← 对齐
sed -n '/知识地图/,/---/p' README.md | head -55
```

预期：树中新增 `├── gtm/                    ← 发布与推广策略（GTM）`，上下 `│` 分隔行完整，`←` 与相邻行纵向对齐；`└── archive/` 仍是树最后一行。

- [ ] **Step 3: 提交**

```bash
git add README.md
git commit -m "根 README 知识地图加入 gtm/ 导航行"
```

---

## 自查记录

- spec 覆盖：交付物 1 → Task 1（六节逐节对应 spec 第 1-6 条；经验断言标注、引用不复制、门禁清单、双渠道表均落实）；交付物 2 → Task 2；内容原则 3 → Task 1 Step 1；验收标准 5 条 → Task 1 Step 3 + Task 2 Step 2；范围外约束（不建子页、不改 roadmap、不执行发布、不生成素材）→ 两任务均未触及。
- 占位符扫描：gtm/README.md 全文为终稿文本，无 TBD / 「类似 Task N」 / 省略号；步骤均给出实际内容或确切命令。
- 命名一致性：`gtm/README.md`、`../docs/roadmap.md`、`../README.md#30-秒读懂`、`../demo/scenarios/01-扩散策略-桌面堆叠/README.md`、`../docs/conventions.md` 在 Task 1 全文与 Step 3 验证命令中一致；Task 2 的插入文本与 spec 交付物 2 逐字一致（含 20 空格对齐）。
